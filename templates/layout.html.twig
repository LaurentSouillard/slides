{% extends 'base.html.twig' %}

{% block body %}

<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
               
                {% if not is_granted("IS_AUTHENTICATED_FULLY") %} 
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_login') }}"> Connexion </a>
                    </li>
                    {# <li class="nav-item active">
                        <a class="nav-link" href="{#{{ path('app_register') }}#}{#"> Inscription </a>
                    </li> #}
                {% else %} 

                    {#<li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_home') }}"> Accueil </a>
                    </li>#}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_dashboard') }}"> Tableau de bord </a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{#{{ path('app_slide') }}#}"> SlideShow </a>
                    </li>

                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            Admin
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                            <li><a class="dropdown-item" href="{{ path('app_gallery_new') }}">Ajout images</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_categories') }}">Catégories</a></li>
                        </ul>
                    </div> 
                    
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_logout') }}"> Déconnexion </a>
                    </li>
                    
                {% endif %} 

            {###################################### Je garde si jamais #############################################}
              {#  {% if not is_granted("IS_AUTHENTICATED_FULLY") and is_granted('ROLE_ADMIN') %} 
                {% endif %} #}

            </ul>
        </div>
    </nav>
</header>


{% block content %}

{################### CE BLOCK RESTERA VIDE DANS LE LAYOUT == IL FAUDRA INSéRER LE CONTENU PROPRE à CHAQUE PAGE #############################################}
   
   
{% endblock %}

{% endblock %}