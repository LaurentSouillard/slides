{% extends 'layout.html.twig' %}

{% block title %}Tableau de bord{% endblock %}

{% block content %}

    {% if not is_granted("IS_AUTHENTICATED_FULLY") %} 
        <h3 class="heading-1 text-center m-5">Merci de vous connecter pour accéder au tableau de bord du Slide Show Eversys</h3>
    {% else %}

    {% for user in users %} 
        <div class="d-flex justify-content-center align-items-center m-5">
            <div class="alert alert-success">
                <p>Vous êtes actuellement connecter en tant que {{user.email}}</p>
            </div>
        </div>       
    {% endfor %}
        {#<h1 class="heading-1 text-center m-5">Images</h1>

        {% if images is empty %}
            <div class="row d-flex justify-content-center text-center">
                <div class="alert alert-danger col-md-5">
                    <p>Pas d'images pour le moment, merci d'en ajouter s'il vous plaît...</p>
                </div>
            </div>

        {% else %}

        <div class="row d-flex justify-content-center text-center">

            {#<div class="col-md-3 btn-group mb-3">
                <button class="btn btn-sm btn-info"><a href="{{ path('app_dashboard') }}" target="_blank" style="text-decoration: none; color: #ffffff;">Dashboard</a></button>
                <button class="btn btn-sm btn-success"><a href="{{ path('app_slide') }}" target="_blank" style="text-decoration: none; color: #ffffff;">Voir le Slide Show</a></button>
            </div>#}

            {#<div class="row d-flex justify-content-center">

                <div class="d-flex justify-content-center col-md-3">

                   {# {{ form_start(formSearch) }}

                        <h4 class="text-start m-3">Catégories :</h4>

                        {{ form_row(formSearch.categories) }}

                        <button type="submit" class="btn btn-primary w-100">Filtrer</button>

                    {{ form_end(formSearch) }} #}

                {#</div> #}

                {#<div class="row d-flex justify-content-center col-md-9">

                    {% for image in images | reverse %}

                        <div class="card m-1 p-1 bg-dark" style="width: 12rem; height: 12rem;">
                            <div class="card-body mt-3">

                                <img src="{{ asset('/img/'~image.nom) }}" alt="" width="125">

                                <div class="d-flex justify-content-center align-items-center mt-3">
                                    <h6 class="card-title m-2 text-light">{{ image.id }}</h6>
                                    <a href="{{ path('app_image_show', {id: image.id}) }}"><button class="btn btn-sm p-1"><i class="fas fa-eye mx-1 text-primary" title="voir"></i></button></a>
                                    <a href=" {{ path('app_image_delete', {id: image.id}) }} "><button class="btn btn-sm p-1"><i class="fas fa-trash mx-1 text-danger" title="supprimer"
                                    onclick=" return confirm('Voulez-vous vraiment supprimer cette image ?') "></i></button></a>
                                    <span class="m-2 badge rounded-pill bg-light text-dark">{#{{ image.categorie.titre }}#}{#</span>

                                    {% if image.inSlide == true %} 
                                        <span class="position-absolute top-0 start-100 translate-middle p-2 bg-success border border-light rounded-circle"></span>
                                    {% else %}
                                        <span class="position-absolute top-0 start-100 translate-middle p-2 bg-danger border border-light rounded-circle"></span>
                                    {% endif %}
                                    
                                </div> 

                            </div>
                        </div>

                    {% endfor %}
                    
                    <div class="navigation d-flex justify-content-center m-3">
                        {#{{ knp_pagination_render(images) }}#}
                    {#</div>

                </div>

            </div>
        </div>#}
        {#{% endif %}#} 

    {% endif %} 

{% endblock %}

